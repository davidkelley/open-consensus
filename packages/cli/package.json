{
  "name": "@open-consensus/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "open-consensus": "./dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@open-consensus/config": "*",
    "@open-consensus/core": "*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "vitest": "^2.1.8"
  }
}
