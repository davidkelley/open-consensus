#!/usr/bin/env node
// Entry stub for the `open-consensus` binary (CLI + slash-command TUI launcher).
// The real implementation lands in Stages 8 (CLI + command-core) and 9 (TUI).
console.log(
  'open-consensus: scaffold. CLI + TUI not yet implemented. See .design/PLAN-open-consensus.md',
)
