{
  "name": "@open-consensus/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "open-consensus-mcp": "./dist/mcp.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "vitest": "^2.1.8"
  }
}
